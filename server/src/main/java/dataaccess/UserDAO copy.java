package dataaccess;

import dataaccess.DataAccessException;
import model.*;

public interface UserDAO {

    //These are some of the functions that we may want to implement:
    // clear, createUser, getUser
}
